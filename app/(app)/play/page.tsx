// Hub "Jouer" — route minimale pour que l'onglet ne renvoie pas de 404.
// Écran réel (Matchs / Bracket / Paris / Mes pronos, T6a §3) : lot suivant.
export default function PlayPage() {
  return <p>Jouer — à venir.</p>;
}
